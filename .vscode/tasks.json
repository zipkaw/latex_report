{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile XeLaTeX documents",
            "type": "shell",
            "command": "${workspaceFolder}/compile.bat",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Update VSCode PDF view",
            "command": "${command:latex-workshop.refresh-viewer}",
        },
    ]
}
